{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--config",
        "${workspaceFolder}/.cursor/mcp.config.json"
      ]
    }
  }
}
